import React from 'react'
import { Input } from 'semantic-ui-react'

export const DocumentIOSearch = () => {
	return (
		<Input loading className='icon' icon='search' placeholder='Поиск' style={{width: '100%'}} />
	)
}